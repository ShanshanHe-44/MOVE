# DO NOT EDIT DEFAULTS

defaults:
  - base_data

# FEEL FREE TO EDIT BELOW

raw_data_path: data/              # where raw data is stored
interim_data_path: interim_data_cat/  # where intermediate files will be stored
results_path: results_cat/            # where result files will be placed

sample_names: random.catagorical.ids    # names/IDs of each sample, must appear in
                                  # the other datasets

categorical_inputs:               # a list of categorical datasets
  - name: random.catagorical.drugs
  - name: random.catagorical.metagenomics

continuous_inputs: []              # no continuous inputs